# Composer — 简谱音乐创作工具

> 用简谱数字自由创作你的音乐 — 多音轨 · 12 种乐器 · 电子合成 · 导出 MP3

[在线体验](https://jerry520-yes.github.io/composer) · [项目主页](https://jerry520-yes.github.io/)

## 核心功能

### 多音轨编辑
- 无限多音轨并行编辑，每条音轨独立配置乐器、音量（可拖动滑块实时调节）、独奏/静音
- 各音轨独立设置节拍号、速度（BPM）和调号，不同速度的音轨可同时播放
- 多音轨时间轴严格对齐，左侧显示音轨节拍/调号徽标
- 一键复制整段旋律到其他乐器音轨（Ctrl+A/Ctrl+C/Ctrl+V）

### 12 种乐器音色
原声（钢琴、吉他、小提琴、长笛、贝斯、小号、古筝）、打击（架子鼓）、电子（合成器、电钢琴、铺底合成、主音合成）——全部由 Web Audio API 代码合成，零音频文件依赖。

### 音符输入与时值
- 数字键 `1`–`7` 输入音符，`0` 休止符，`←`/`→` 移动光标，`↑`/`↓` 升降八度
- 六种时值（全音、二分、四分、八分、十六分、六分），附点模式（`.` 切换）、升调模式（`,` 切换）
- 选中音符后点击时值按钮可批量修改选中区域音符的时值、附点和升调属性

### 连音
- Ctrl + 鼠标拖拽选中连续音符，点击「连音」添加 SVG 贝塞尔曲线连音线，点击连音线可取消

### 播放与导出
- 空格键播放/停止，实时音符高亮跟随，内置节拍器
- 导出 MP3（OfflineAudioContext 渲染 + lamejs 编码）
- 项目保存/加载（JSON 格式，含音符、连音、段落标记、各音轨独立配置等全部数据）

### 段落标记
- 自定义段落标签（如「A段」「副歌」「间奏」），快速导航

## 技术栈
- **Web Audio API**：实时音频合成与播放 + OfflineAudioContext 离线渲染导出
- **纯前端单文件**：无需后端，浏览器即开即用，响应式布局支持桌面和移动端

## 版权声明
可免费使用，使用时需标注水印。
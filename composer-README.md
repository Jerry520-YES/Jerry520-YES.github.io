# Composer — 简谱音乐创作工具

> 用简谱数字自由创作你的音乐 — 多音轨 · 12 种乐器 · 电子合成 · 导出 MP3

[在线体验](https://jerry520-yes.github.io/composer) · [项目主页](https://jerry520-yes.github.io/)

## 核心特性

### 多音轨编辑
- 支持无限多音轨并行编辑，每条音轨独立配置乐器、音量、独奏/静音
- **各音轨独立节拍/速度/调号**：每条音轨可独立设置节拍号（4/4、3/4、2/4、6/8）、速度（BPM）和调号（C、G、D、F），不同速度的音轨可同时播放
- 多音轨时间轴严格对齐，可视化并行预览，左侧显示音轨节拍/调号徽标
- 一键复制整段旋律到其他乐器音轨（`Ctrl+A` 全选 → `Ctrl+C` 复制 → 切换音轨 → `Ctrl+V` 粘贴）

### 12 种乐器音色
| 分类 | 乐器 | 合成方式 |
|------|------|----------|
| 原声 | 钢琴、吉他、小提琴、长笛、贝斯、小号、古筝 | 多谐波叠加 + 包络整形 |
| 打击 | 架子鼓 | 噪声合成 + 滤波 |
| 电子 | 合成器、电钢琴、铺底合成、主音合成 | 减法合成 / FM / unison |

### 音符输入与时值
- **键盘输入**：数字键 `1`–`7` 输入音符，`0` 输入休止符
- **六种时值**：全音、二分、四分、八分、十六分、六分（`2×3` 网格排列）
- **附点模式**：按 `.` 键切换附点模式，音符自动添加附点延长时值
- **升调模式**：按 `,` 键切换升调模式，音符自动升半音（适用于所有乐器）
- **八度控制**：`↑`/`↓` 调整八度（-2 ~ +2），支持上下八度点标记
- **选中批量修改时值**：`Ctrl` + 鼠标选中一段音符后，点击时值按钮即可批量修改选中音符的时值、附点和升调属性

### 连音功能
- `Ctrl` + 鼠标拖拽选中连续音符
- 点击「连音」按钮添加连音线（SVG 贝塞尔曲线渲染）
- 点击连音线可取消连音
- 连音数据随项目保存/加载

### 播放与导出
- 空格键控制播放/停止，停止后重新播放自动清空旧记录从头开始
- 实时音符高亮跟随
- 内置节拍器（可开关）
- 支持导出 MP3 文件（OfflineAudioContext 渲染 + 编码）
- 项目保存/加载（JSON 格式，含音符、连音、段落标记、各音轨独立配置等全部数据）

### 段落标记
- 自定义段落标签（如「A段」「副歌」「间奏」）
- 段落列表快速导航

## 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `1`–`7` | 输入对应音高 |
| `0` | 输入休止符 |
| `←` / `→` | 移动光标 |
| `↑` / `↓` | 升降八度 |
| `.` | 切换附点模式 |
| `,` | 切换升调模式 |
| `⌫` | 删除当前音符或选中区域 |
| `Ctrl` + `C` | 复制选中音符 |
| `Ctrl` + `V` | 粘贴音符到光标处 |
| `Ctrl` + `A` | 全选当前音轨所有音符 |
| `Ctrl` + 鼠标拖拽 | 选中连续音符区域 |
| `空格` | 播放 / 停止 |

## 技术栈

- **Web Audio API**：实时音频合成与播放，无任何音频文件依赖，全部由代码合成
- **OfflineAudioContext**：离线渲染用于 MP3 导出
- **纯前端**：单文件 HTML 应用，无需后端，浏览器即开即用
- **响应式布局**：支持桌面和移动端，自适应三栏/单栏布局

## 项目结构

```
composer.html    # 单文件应用，包含全部 HTML/CSS/JS
```

## 版权声明

可免费使用，不开源，使用时需要标注水印。